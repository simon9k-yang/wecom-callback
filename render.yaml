services:
  - type: web
    name: wecom-function-lab
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: WECOM_TOKEN
        sync: false
